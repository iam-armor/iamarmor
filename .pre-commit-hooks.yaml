- id: iamarmor
  name: iamarmor (IAM policy scanner)
  description: Scan Terraform IAM policies for over-permissioned configs
  entry: iamarmor scan
  language: python
  types: [terraform]
  pass_filenames: false
  args: ["--format", "text"]
